# seamless-share
Tools for the sharing, caching, auditing and cache control of Seamless buffers and transformations

STATUS: this repo is in a vibe-coded state, untested by humans. It also depends on the (equally vibe-coded) `transformation-is-cached` branch of `seamless-transformer`.

`./usage-patterns.md` was written by Opus to reflect its understanding of what `seamless-share` is/will be used for. Not vetted by the Seamless creator or any pother human. Take with a big grain of salt.